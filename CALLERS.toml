schema_version = 2
plan_id = "HEPTA-GLOBAL-MODULAR-DEVELOPMENT-PLAN"
plan_version = "8.0.0"
source_roots = ["codex-rs"]
ignored_path_fragments = ["/tests/", "/examples/", "_tests.rs"]
rule = "Every privileged Hepta boundary has an exact closed set of product call sites. Test, example and definition-only uses do not grant production authority."

[[boundary]]
id = "supervisor_authorized_boot"
symbol = "run_supervisord_with_grant_verifier"
definition_path = "codex-rs/hepta-supervisor/src/daemon.rs"
definition_markers = ["pub async fn run_supervisord_with_grant_verifier", "GrantVerifier"]
product_callers = ["codex-rs/hepta-supervisor/src/main.rs"]
caller_markers = ["run_supervisord_with_grant_verifier", "H7H89ProductionGrantVerifier"]

[[boundary]]
id = "memory_production_writer_open"
symbol = "ProductionDurableWriter::open"
definition_path = "codex-rs/hepta-memory/src/production_writer.rs"
definition_markers = ["pub struct ProductionDurableWriter", "pub async fn open", "ProductionAuthorityVerifier"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["ProductionDurableWriter::open", "verifier"]

[[boundary]]
id = "memory_qualified_compact_checkpoint_publish"
symbol = "ProductionDurableWriter::publish_qualified_compact_checkpoint"
definition_path = "codex-rs/hepta-memory/src/production_writer.rs"
definition_markers = ["pub async fn publish_qualified_compact_checkpoint", "QualifiedCompactCheckpointPublication"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["publish_qualified_compact_checkpoint", "build_qualified_candidate", "prove_compaction"]

[[boundary]]
id = "memory_qualified_compact_current_select"
symbol = "CognitiveStore::select_current_qualified_compact_checkpoint"
definition_path = "codex-rs/hepta-memory/src/qualified_compact_store.rs"
definition_markers = ["pub async fn select_current_qualified_compact_checkpoint", "revalidate_current_publication", "resolve_qualified_compact_payload"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["select_current_compaction_checkpoint", "select_current_qualified_compact_checkpoint", "revalidate_compaction_trust"]


[[boundary]]
id = "memory_qualified_compact_rollback_candidate"
symbol = "CognitiveStore::rollback_qualified_compact_payload_candidate"
definition_path = "codex-rs/hepta-memory/src/qualified_compact_store.rs"
definition_markers = ["pub async fn rollback_qualified_compact_payload_candidate", "revalidate_current_publication", "resolve_qualified_compact_payload"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["rollback_compaction_payload_candidate", "rollback_qualified_compact_payload_candidate", "revalidate_compaction_trust"]

[[boundary]]
id = "memory_qualified_compact_payload_revoke"
symbol = "ProductionDurableWriter::revoke_qualified_compact_payload"
definition_path = "codex-rs/hepta-memory/src/production_writer.rs"
definition_markers = ["pub async fn revoke_qualified_compact_payload", "revoke_qualified_compact_payload"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["revoke_compaction_payload", "revoke_qualified_compact_payload"]

[[boundary]]
id = "memory_qualified_compact_payload_gc"
symbol = "ProductionDurableWriter::gc_revoked_qualified_compact_payload"
definition_path = "codex-rs/hepta-memory/src/production_writer.rs"
definition_markers = ["pub async fn gc_revoked_qualified_compact_payload", "gc_revoked_qualified_compact_payload"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["gc_revoked_compaction_payload", "gc_revoked_qualified_compact_payload"]

[[boundary]]
id = "compact_engine_authoritative_snapshot_final_use"
symbol = "AuthoritativeCognitiveSnapshotProvider::acquire"
definition_path = "codex-rs/hepta-cognitive-read/src/authoritative.rs"
definition_markers = ["pub trait AuthoritativeCognitiveSnapshotProvider", "fn acquire"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["attach_snapshot_provider", "acquire_current_snapshot", "provider.acquire"]


[[boundary]]
id = "compact_engine_trusted_tokenizer_accounting"
symbol = "build_qualified_candidate"
definition_path = "codex-rs/hepta-compact-engine/src/qualified.rs"
definition_markers = ["pub fn build_qualified_candidate", "TrustedTokenizerV1", "TokenizationReceiptV1", "verify_strict"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["build_qualified_candidate", "compaction_trust", "TrustedTokenizerV1"]

[[boundary]]
id = "compact_engine_trusted_evaluator_proof"
symbol = "prove_compaction"
definition_path = "codex-rs/hepta-compact-engine/src/qualified.rs"
definition_markers = ["pub fn prove_compaction", "TrustedCompactionEvaluatorV1", "verify_strict"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["prove_compaction", "compaction_trust"]


[[boundary]]
id = "agentd_production_writer_host"
symbol = "AgentdProductionWriterBootstrap::open_with_store"
definition_path = "codex-rs/hepta-agentd/src/production_writer_host.rs"
definition_markers = ["pub struct AgentdProductionWriterBootstrap", "pub async fn open_with_store", "ProductionAuthorityVerifier"]
product_callers = ["codex-rs/hepta-agentd/src/runtime.rs"]
caller_markers = ["production_writer_bootstrap", "attach_production_writer_after_generation_fence", "bootstrap.open_with_store"]

[[boundary]]
id = "provider_effect_coordinator"
symbol = "ProviderEffectCoordinator::new"
definition_path = "codex-rs/hepta-contracts/src/provider_effect.rs"
definition_markers = ["pub struct ProviderEffectCoordinator", "pub fn new", "ProviderEffectAdapter"]
product_callers = []
caller_markers = []

[[boundary]]
id = "http_provider_effect_adapter"
symbol = "HttpProviderEffectAdapter::new"
definition_path = "codex-rs/model-provider/src/provider_effect.rs"
definition_markers = ["pub struct HttpProviderEffectAdapter", "pub fn new", "ProviderEffectAdapter"]
product_callers = []
caller_markers = []

[[protected_file]]
path = "codex-rs/hepta-supervisor/src/main.rs"
required = ["run_supervisord_with_grant_verifier", "H7H89ProductionGrantVerifier", "run_supervisord"]
forbidden = ["H7H89ProductionGrantVerifier::default"]

[[protected_file]]
path = "codex-rs/hepta-agentd/src/production_writer_host.rs"
required = ["ProductionDurableWriter::open", "ProductionAuthorityVerifier", "AgentdCompactionTrustV1", "TrustedTokenizerV1", "attach_compaction_trust", "attach_snapshot_provider", "acquire_current_snapshot", "revalidate_compaction_trust", "CompactionTrustDrift", "select_current_compaction_checkpoint", "rollback_compaction_payload_candidate", "revoke_compaction_payload", "gc_revoked_compaction_payload"]
forbidden = ["AllowVerifier", "permit_all", "TrustedCompactionEvaluatorV1::default", "TrustedTokenizerV1::default"]

[[protected_file]]
path = "codex-rs/hepta-agentd/src/runtime.rs"
required = ["production_writer_bootstrap", "attach_production_writer_after_generation_fence", "rollback_lease"]
forbidden = ["ProductionAuthorityLease::from_verified_parts", "ProductionAuthorityToken::from_verified_bytes"]

[[protected_file]]
path = "codex-rs/model-provider/src/provider_effect.rs"
required = ["impl ProviderEffectAdapter for HttpProviderEffectAdapter"]
forbidden = ["impl Default for HttpProviderEffectAdapter"]

[authority]
runtime_authority = false
production_caller = false
production_writer = false
external_effect = false
self_issued_acceptance = false
promotion = false
release = false
