schema_version = 2
plan_id = "HEPTA-GLOBAL-MODULAR-DEVELOPMENT-PLAN"
plan_version = "8.0.0"
source_roots = ["codex-rs"]
ignored_path_fragments = ["/tests/", "/examples/", "_tests.rs"]
rule = "Every privileged Hepta boundary has an exact closed set of product call sites. Test, example and definition-only uses do not grant production authority."

[privileged_inventory]
required_boundary_ids = [
  "supervisor_authorized_boot",
  "cognitive_store_recovery_open",
  "cognitive_production_mutation_capability",
  "learning_artifact_owner_bootstrap",
  "learning_artifact_owner_restart",
  "learning_artifact_verified_current_view",
  "memory_production_writer_open",
  "agentd_production_writer_host",
  "agentd_production_operation_runtime",
  "provider_effect_coordinator",
  "http_provider_effect_adapter",
  "final_use_open_state",
  "final_use_claim",
  "final_use_claim_raw",
  "final_use_delivery",
  "final_use_delivery_witness",
  "final_use_delivery_raw",
  "final_use_dispatch",
  "final_use_dispatch_witness",
  "final_use_dispatch_raw",
  "final_use_revocation_update",
  "final_use_approval_trust",
  "final_use_approval_verify",
  "final_use_revocation_feed_trust",
  "final_use_revocation_feed_verify",
  "final_use_revocation_feed_apply",
  "final_use_revocation_convergence_trust",
  "final_use_revocation_convergence_verify",
  "bao_final_use_consumer",
  "bao_registered_consumer_new",
  "bao_final_use_host_new",
  "bao_registered_revocation_update",
  "bao_registered_final_use_host",
  "authority_lease_open_state",
  "authority_lease_open_production_state",
  "authority_lease_put",
  "authority_lease_revoke",
  "authority_lease_prune",
  "authority_lease_epoch_advance",
  "authority_lease_verify_use",
  "authority_lease_bind_dispatch",
  "authority_lease_delivery",
  "authority_lease_delivery_witness",
  "authority_lease_dispatch_raw",
  "authority_lease_dispatch_witness",
  "authority_lease_dispatch_binding",
  "final_use_async_dispatch_fence",
  "final_use_guarded_effect",
  "final_use_async_entry",
  "bao_authbus_final_use_consumer",
  "automation_taskflow_provider_effect_bridge"
]

[[boundary]]
id = "supervisor_authorized_boot"
symbol = "run_supervisord_with_grant_verifier"
definition_path = "codex-rs/hepta-supervisor/src/daemon.rs"
definition_markers = ["pub async fn run_supervisord_with_grant_verifier", "GrantVerifier"]
product_callers = ["codex-rs/hepta-supervisor/src/main.rs"]
caller_markers = ["run_supervisord_with_grant_verifier", "H7H89ProductionGrantVerifier"]

[[boundary]]
id = "memory_production_writer_open"
symbol = "ProductionDurableWriter::open_with_live_verifier"
definition_path = "codex-rs/hepta-memory/src/production_writer.rs"
definition_markers = ["pub struct ProductionDurableWriter", "pub async fn open_with_live_verifier", "ProductionAuthorityVerifier"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["ProductionDurableWriter::open_with_live_verifier", "verifier"]

[[boundary]]
id = "cognitive_store_recovery_open"
symbol = "CognitiveStore::open_with_recovery"
definition_path = "codex-rs/hepta-memory/src/cognitive_store_recovery.rs"
definition_markers = ["pub async fn open_with_recovery", "ProductionAuthorityVerifier", "CognitiveStoreOpenGuard::acquire_exclusive"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["CognitiveStore::open_with_recovery", "ProductionAuthorityVerifier"]

[[boundary]]
id = "cognitive_production_mutation_capability"
symbol = "ProductionDurableWriter::cognitive_mutation_capability"
call_pattern = '\.\s*cognitive_mutation_capability\s*\('
definition_path = "codex-rs/hepta-memory/src/production_writer.rs"
definition_markers = ["pub struct ProductionCognitiveMutationCapability", "pub fn cognitive_mutation_capability", "production_cognitive_mutation_sealed"]
product_callers = ["codex-rs/hepta-agentd/src/production_writer_host.rs"]
caller_markers = ["cognitive_mutation_capability", "production_mutation"]

[[boundary]]
id = "agentd_production_writer_host"
symbol = "AgentdProductionWriterHost::open"
definition_path = "codex-rs/hepta-agentd/src/production_writer_host.rs"
definition_markers = ["pub struct AgentdProductionWriterHost", "pub async fn open", "CognitiveRecoveryRequirement"]
product_callers = []
caller_markers = []

[[boundary]]
id = "agentd_production_operation_runtime"
symbol = "AgentdProductionOperationRuntimeConfig::attach"
definition_path = "codex-rs/hepta-agentd/src/production_writer_host.rs"
definition_markers = ["pub struct AgentdProductionOperationRuntimeConfig", "pub(crate) async fn attach", "recovered_host.writer.verify_current_authority"]
product_callers = []
caller_markers = []

[[boundary]]
id = "learning_artifact_owner_bootstrap"
symbol = "LearningArtifactOwnerHost::open"
definition_path = "codex-rs/hepta-learning-artifacts/src/owner_host.rs"
definition_markers = ["pub struct LearningArtifactOwnerHost", "pub fn open", "SignedArtifactWriterLeaseV1"]
product_callers = ["codex-rs/hepta-learning-artifacts/src/owner_service.rs"]
caller_markers = ["LearningArtifactOwnerHost::open", "LearningArtifactOwnerService"]

[[boundary]]
id = "learning_artifact_owner_restart"
symbol = "LearningArtifactOwnerHost::open_with_required_current_head"
definition_path = "codex-rs/hepta-learning-artifacts/src/owner_host.rs"
definition_markers = ["pub fn open_with_required_current_head", "CurrentHeadRollback", "required_current_head"]
product_callers = ["codex-rs/hepta-learning-artifacts/src/owner_service.rs"]
caller_markers = ["LearningArtifactOwnerHost::open_with_required_current_head", "LearningArtifactOwnerService"]

[[boundary]]
id = "learning_artifact_verified_current_view"
symbol = "VerifiedCurrentRegistryViewV1"
call_pattern = '\.\s*with_current\s*\('
definition_path = "codex-rs/hepta-learning-artifacts/src/pinned.rs"
definition_markers = ["pub struct VerifiedCurrentRegistryViewV1", "pub fn with_current", "with_verified_registry"]
product_callers = [
  "codex-rs/hepta-agentd/src/cognitive_ranker.rs",
  "codex-rs/hepta-intelligence/src/ndu_stochastic_admission.rs",
]
caller_markers = ["VerifiedCurrentRegistryViewV1", "with_current"]

[[boundary]]
id = "provider_effect_coordinator"
symbol = "ProviderEffectCoordinator::new"
definition_path = "codex-rs/hepta-contracts/src/provider_effect.rs"
definition_markers = ["pub struct ProviderEffectCoordinator", "pub fn new", "ProviderEffectAdapter"]
product_callers = []
caller_markers = []

[[boundary]]
id = "http_provider_effect_adapter"
symbol = "HttpProviderEffectAdapter::new"
definition_path = "codex-rs/model-provider/src/provider_effect.rs"
definition_markers = ["pub struct HttpProviderEffectAdapter", "pub fn new", "ProviderEffectAdapter"]
product_callers = ["codex-rs/hepta-agentd/src/automation_effect_host.rs"]
caller_markers = ["HttpProviderEffectAdapter::new", "dispatch_with_payload", "lookup_for_intent"]

[[boundary]]
id = "final_use_open_state"
symbol = "FinalUseAuthority::open_state_dir"
call_pattern = "(?:FinalUseAuthority\\s*::\\s*open_state_dir(?:_with_clock|_with_trust|_with_issuer_keys)?\\s*\\(|FinalUseAuthority\\s*::\\s*recover_state_dir_with_issuer_keys\\s*\\()"
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn open_state_dir(", "pub fn open_state_dir_with_issuer_keys(", "InvalidTrust", "Store::open"]
product_callers = ["codex-rs/hepta-agentd/src/bin/hepta-agentd-browser.rs", "codex-rs/hepta-agentd/src/automation_effect_host.rs", "codex-rs/hepta-infer-worker-host/src/final_use_authorizer.rs"]
caller_markers = ["FinalUseAuthority::", "FinalUseAuthority", "authority"]

[[boundary]]
id = "final_use_claim"
symbol = "claim_final_use"
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn claim_final_use(", "authority.claim(signed, expected)"]
product_callers = ["codex-rs/hepta-bao-adapter/src/https_consumer.rs", "codex-rs/hepta-agentd/src/browser_servo.rs"]
caller_markers = ["claim_final_use"]

[[boundary]]
id = "final_use_claim_raw"
symbol = "FinalUseAuthority::claim"
call_pattern = '(?:authority|final_use|self\s*\.\s*authority)\s*\.\s*claim\s*\('
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn claim(", "verify_strict", "used_nonces"]
product_callers = ["codex-rs/hepta-automation/src/authorized_effect.rs", "codex-rs/hepta-infer-worker-host/src/final_use_authorizer.rs", "codex-rs/hepta-memory/src/production_writer.rs", "codex-rs/hepta-ndu/src/owner.rs", "codex-rs/hepta-operations/src/durable_store.rs", "codex-rs/hepta-prompt-registry/src/admission.rs", "codex-rs/hepta-prompt-registry/src/durable.rs", "codex-rs/hepta-runtime/src/organs.rs"]
caller_markers = ["FinalUseAuthority", "SignedFinalUseGrant", ".claim("]

[[boundary]]
id = "final_use_delivery"
symbol = "deliver_final_use"
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn deliver_final_use", "authority.with_verified_use(token, expected, consumer)"]
product_callers = ["codex-rs/hepta-bao-adapter/src/https_consumer.rs"]
caller_markers = ["deliver_final_use", "deliver_final_use(authority, verified, &binding", "ConsumerIndeterminate"]

[[boundary]]
id = "final_use_delivery_witness"
symbol = "deliver_final_use_with_witness"
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn deliver_final_use_with_witness", "VerifiedUseTokenWitnessV1", "ConsumerEntry"]
product_callers = []
caller_markers = []

[[boundary]]
id = "final_use_delivery_raw"
symbol = "FinalUseAuthority::with_verified_use"
call_pattern = '(?:authority|final_use|self\s*\.\s*authority)\s*\.\s*with_verified_use\s*\('
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn with_verified_use", "Arc::ptr_eq", "validate_live"]
product_callers = ["codex-rs/hepta-memory/src/production_writer.rs", "codex-rs/hepta-ndu/src/owner.rs", "codex-rs/hepta-operations/src/durable_store.rs", "codex-rs/hepta-prompt-registry/src/admission.rs", "codex-rs/hepta-prompt-registry/src/durable.rs", "codex-rs/hepta-runtime/src/organs.rs"]
caller_markers = ["FinalUseAuthority", ".claim(", "with_verified_use"]

[[boundary]]
id = "final_use_dispatch"
symbol = "dispatch_final_use"
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn dispatch_final_use", "HEPTA_PRIVILEGED_BOUNDARY_FINAL_USE_DISPATCH", "with_dispatch_boundary"]
product_callers = ["codex-rs/hepta-agentd/src/browser_servo.rs"]
caller_markers = ["dispatch_final_use", "authority_enter", "dispatch_boundary"]

[[boundary]]
id = "final_use_dispatch_witness"
symbol = "dispatch_final_use_with_witness"
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn dispatch_final_use_with_witness", "VerifiedUseTokenWitnessV1", "DispatchEntry"]
product_callers = []
caller_markers = []

[[boundary]]
id = "final_use_dispatch_raw"
symbol = "FinalUseAuthority::with_dispatch_boundary"
call_pattern = "authority\\s*\\.\\s*with_dispatch_boundary\\s*\\("
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn with_dispatch_boundary", "validate_live", "dispatch_boundary"]
product_callers = []
caller_markers = []

[[boundary]]
id = "final_use_revocation_update"
symbol = "FinalUseAuthority::update_revocations"
call_pattern = "authority\\s*\\.\\s*update_revocations\\s*\\("
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn update_revocations", "StaleRevocationHead", "store.persist"]
product_callers = ["codex-rs/hepta-contracts/src/final_use_control.rs", "codex-rs/hepta-infer-worker-host/src/final_use_authorizer.rs"]
caller_markers = ["FinalUseAuthority", "update_revocations", "FinalUseRevocations"]

[[boundary]]
id = "final_use_approval_trust"
symbol = "FinalUseApprovalVerifier::new"
definition_path = "codex-rs/hepta-contracts/src/final_use_control.rs"
definition_markers = ["impl FinalUseApprovalVerifier", "pub fn new(", "InvalidTrust"]
product_callers = []
caller_markers = []

[[boundary]]
id = "final_use_approval_verify"
symbol = "FinalUseApprovalVerifier::verify"
call_pattern = "approval_verifier\\s*\\.\\s*verify\\s*\\("
definition_path = "codex-rs/hepta-contracts/src/final_use_control.rs"
definition_markers = ["pub struct FinalUseApprovalVerifier", "pub fn verify(", "verify_strict"]
product_callers = ["codex-rs/hepta-bao-adapter/src/final_use_host.rs"]
caller_markers = ["approval_verifier", ".verify(grant, approval)", "BaoFinalUseHostError::Control"]

[[boundary]]
id = "final_use_revocation_feed_trust"
symbol = "FinalUseRevocationFeedVerifier::new"
call_pattern = 'FinalUseRevocationFeedVerifier\s*::\s*(?:new|new_with_keys)\s*\('
definition_path = "codex-rs/hepta-contracts/src/final_use_control.rs"
definition_markers = ["impl FinalUseRevocationFeedVerifier", "pub fn new(", "pub fn new_with_keys(", "InvalidTrust"]
product_callers = ["codex-rs/hepta-agentd/src/automation_effect_host.rs"]
caller_markers = ["FinalUseRevocationFeedVerifier::new_with_keys", "final_use_revocation_keys"]

[[boundary]]
id = "final_use_revocation_feed_verify"
symbol = "FinalUseRevocationFeedVerifier::verify"
call_pattern = 'revocation_feed_verifier\s*\.\s*verify\s*\('
definition_path = "codex-rs/hepta-contracts/src/final_use_control.rs"
definition_markers = ["pub fn verify<", "self.verify_signed(signed, now_unix_ms)"]
product_callers = ["codex-rs/hepta-agentd/src/automation_effect_host.rs"]
caller_markers = ["revocation_feed_verifier", ".verify(&signed", "signed final-use revocation feed rejected"]

[[boundary]]
id = "final_use_revocation_feed_apply"
symbol = "FinalUseRevocationFeedVerifier::apply"
call_pattern = 'revocation_(?:feed_)?verifier\s*\.\s*apply\s*\('
definition_path = "codex-rs/hepta-contracts/src/final_use_control.rs"
definition_markers = ["pub struct FinalUseRevocationFeedVerifier", "pub fn apply(", "update_revocations"]
product_callers = [
  "codex-rs/hepta-agentd/src/automation_effect_host.rs",
  "codex-rs/hepta-bao-adapter/src/final_use_host.rs",
]
caller_markers = ["FinalUseRevocationFeedVerifier", ".apply("]

[[boundary]]
id = "final_use_revocation_convergence_trust"
symbol = "FinalUseRevocationConvergenceVerifier::new"
definition_path = "codex-rs/hepta-contracts/src/final_use_control.rs"
definition_markers = ["pub struct FinalUseRevocationConvergenceVerifier", "pub fn new(", "InvalidRevocationNodeTrust"]
product_callers = []
caller_markers = []

[[boundary]]
id = "final_use_revocation_convergence_verify"
symbol = "FinalUseRevocationConvergenceVerifier::verify"
call_pattern = "convergence_verifier\\s*\\.\\s*verify\\s*\\("
definition_path = "codex-rs/hepta-contracts/src/final_use_control.rs"
definition_markers = ["pub struct FinalUseRevocationConvergenceVerifier", "pub fn verify(", "missing_nodes"]
product_callers = ["codex-rs/hepta-fleet/src/revocation_control.rs"]
caller_markers = ["convergence_verifier", ".verify(", "FleetRevocationCoordinator"]

[[boundary]]
id = "bao_final_use_consumer"
symbol = "BaoClient::consume_kv_v2"
call_pattern = "\\.\\s*consume_kv_v2\\s*\\("
caller_type_marker = "BaoClient"
definition_path = "codex-rs/hepta-bao-adapter/src/https_consumer.rs"
definition_markers = ["pub async fn consume_kv_v2", "claim_final_use", "deliver_final_use"]
product_callers = []
caller_markers = []

[[boundary]]
id = "bao_registered_consumer_new"
symbol = "RegisteredBaoConsumer::new"
definition_path = "codex-rs/hepta-bao-adapter/src/final_use_host.rs"
definition_markers = ["impl RegisteredBaoConsumer", "pub fn new(", "ConsumerId"]
product_callers = []
caller_markers = []

[[boundary]]
id = "bao_final_use_host_new"
symbol = "BaoFinalUseHost::new"
definition_path = "codex-rs/hepta-bao-adapter/src/final_use_host.rs"
definition_markers = ["impl BaoFinalUseHost", "pub fn new(", "DuplicateConsumer"]
product_callers = []
caller_markers = []

[[boundary]]
id = "bao_registered_revocation_update"
symbol = "BaoFinalUseHost::apply_revocation_update"
call_pattern = "\\.\\s*apply_revocation_update\\s*\\("
definition_path = "codex-rs/hepta-bao-adapter/src/final_use_host.rs"
definition_markers = ["pub fn apply_revocation_update", "revocation_verifier", ".apply(&self.authority, update, now_unix_ms)"]
product_callers = []
caller_markers = []

[[boundary]]
id = "bao_registered_final_use_host"
symbol = "BaoFinalUseHost::consume_kv_v2"
call_pattern = "\\.\\s*consume_kv_v2\\s*\\("
caller_type_marker = "BaoFinalUseHost"
definition_path = "codex-rs/hepta-bao-adapter/src/final_use_host.rs"
definition_markers = ["pub struct BaoFinalUseHost", "pub async fn consume_kv_v2", "approval_verifier", "consumers"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_open_state"
symbol = "AuthorityLeaseRegistry::open_state_dir"
call_pattern = 'AuthorityLeaseRegistry\s*::\s*open_state_dir(?:_with_frontier_store|_with_clock|_with_trust)?\s*\('
definition_path = "codex-rs/hepta-contracts/src/authority_lease.rs"
definition_markers = ["pub fn open_state_dir(", "trusted_frontier", "Store::open"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_open_production_state"
symbol = "AuthorityLeaseRegistry::open_production_state_dir"
call_pattern = 'AuthorityLeaseRegistry\s*::\s*open_production_state_dir\s*\('
definition_path = "codex-rs/hepta-contracts/src/authority_trust.rs"
definition_markers = ["pub fn open_production_state_dir", "ProductionAuthorityTrustEvidence", "ProductionAuthorityClock", "ProductionAuthorityFrontierStore"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_put"
symbol = "AuthorityLeaseRegistry::put_lease"
call_pattern = '\.\s*put_lease\s*\('
caller_type_marker = "AuthorityLeaseRegistry"
definition_path = "codex-rs/hepta-contracts/src/authority_lease.rs"
definition_markers = ["pub struct AuthorityLeaseRegistry", "pub fn put_lease(", "expected_revision"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_revoke"
symbol = "AuthorityLeaseRegistry::revoke"
call_pattern = '\.\s*revoke\s*\('
caller_type_marker = "AuthorityLeaseRegistry"
definition_path = "codex-rs/hepta-contracts/src/authority_lease.rs"
definition_markers = ["pub fn revoke(", "RevocationReceipt", "reason_sha256"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_prune"
symbol = "AuthorityLeaseRegistry::prune_expired_leases"
call_pattern = '\.\s*prune_expired_leases\s*\('
caller_type_marker = "AuthorityLeaseRegistry"
definition_path = "codex-rs/hepta-contracts/src/authority_lease.rs"
definition_markers = ["pub fn prune_expired_leases(", "MAX_AUTHORITY_PRUNE_BATCH", "expires_at_unix_ms"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_epoch_advance"
symbol = "AuthorityLeaseRegistry::advance_epoch"
call_pattern = '\.\s*advance_epoch\s*\('
caller_type_marker = "AuthorityLeaseRegistry"
definition_path = "codex-rs/hepta-contracts/src/authority_lease.rs"
definition_markers = ["pub fn advance_epoch(", "new_authority_epoch", "next.leases.clear()"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_verify_use"
symbol = "AuthorityLeaseVerifier::verify_use"
call_pattern = '\.\s*verify_use\s*\('
caller_type_marker = "AuthorityLeaseVerifier"
definition_path = "codex-rs/hepta-contracts/src/authority_lease.rs"
definition_markers = ["pub struct AuthorityLeaseVerifier", "pub fn verify_use(", "LeaseVerifiedUseToken"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_bind_dispatch"
symbol = "AuthorityLeaseVerifier::bind_dispatch"
call_pattern = '\.\s*bind_dispatch\s*\('
caller_type_marker = "AuthorityLeaseVerifier"
definition_path = "codex-rs/hepta-contracts/src/authority_trust.rs"
definition_markers = ["pub fn bind_dispatch(", "AuthorityDispatchBinding", "self.verify_use("]
product_callers = ["codex-rs/hepta-fleet/src/authority_port.rs"]
caller_markers = ["AuthorityDispatchBinding", ".bind_dispatch(", "FleetAuthorityPort"]

[[boundary]]
id = "authority_lease_delivery"
symbol = "AuthorityLeaseVerifier::with_verified_use"
call_pattern = '\.\s*with_verified_use\s*\('
caller_type_marker = "AuthorityLeaseVerifier"
definition_path = "codex-rs/hepta-contracts/src/authority_lease.rs"
definition_markers = ["pub fn with_verified_use", "validate_token_live_witness", "consumer()"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_delivery_witness"
symbol = "deliver_authority_lease_with_witness"
definition_path = "codex-rs/hepta-contracts/src/authority_lease.rs"
definition_markers = ["pub fn deliver_authority_lease_with_witness", "VerifiedUseTokenWitnessV1", "validate_token_live_witness"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_dispatch_raw"
symbol = "AuthorityLeaseVerifier::with_dispatch_boundary"
call_pattern = '\.\s*with_dispatch_boundary\s*\('
caller_type_marker = "AuthorityLeaseVerifier"
definition_path = "codex-rs/hepta-contracts/src/authority_lease.rs"
definition_markers = ["pub fn with_dispatch_boundary", "with_dispatch_boundary_witness", "dispatch_boundary"]
product_callers = []
caller_markers = []

[[boundary]]
id = "authority_lease_dispatch_witness"
symbol = "dispatch_authority_lease_with_witness"
definition_path = "codex-rs/hepta-contracts/src/authority_lease.rs"
definition_markers = ["pub fn dispatch_authority_lease_with_witness", "VerifiedUseTokenWitnessV1", "with_dispatch_boundary_witness"]
product_callers = ["codex-rs/hepta-contracts/src/authority_trust.rs"]
caller_markers = ["dispatch_authority_lease_with_witness", "AuthorityDispatchBinding", "dispatch_boundary"]

[[boundary]]
id = "authority_lease_dispatch_binding"
symbol = "AuthorityDispatchBinding::dispatch"
call_pattern = '\bdispatch_binding\s*\.\s*dispatch\s*\('
caller_type_marker = "AuthorityDispatchBinding"
definition_path = "codex-rs/hepta-contracts/src/authority_trust.rs"
definition_markers = ["pub struct AuthorityDispatchBinding", "pub fn dispatch<T>(", "dispatch_authority_lease_with_witness"]
product_callers = ["codex-rs/hepta-fleet/src/authority_port.rs"]
caller_markers = ["AuthorityDispatchBinding", "dispatch_binding", ".dispatch("]

[[protected_file]]
path = "codex-rs/hepta-supervisor/src/main.rs"
required = ["run_supervisord_with_grant_verifier", "H7H89ProductionGrantVerifier", "run_supervisord"]
forbidden = ["H7H89ProductionGrantVerifier::default"]

[[protected_file]]
path = "codex-rs/hepta-agentd/src/production_writer_host.rs"
required = ["codex_hepta_cognitive_store::DurableCognitiveStore", "ProductionDurableWriter::open_with_live_verifier", "CognitiveStore::open_with_recovery", "ProductionAuthorityVerifier", "cognitive_mutation_capability", "production_mutation"]
forbidden = ["codex_hepta_memory::CognitiveStore", "AllowVerifier", "permit_all", ".writer.store()"]

[[protected_file]]
path = "codex-rs/hepta-agentd/src/runtime.rs"
required = ["codex_hepta_cognitive_store::DurableCognitiveStore", "CognitiveStore::open"]
forbidden = ["use codex_hepta_memory::CognitiveStore", "codex_hepta_memory::CognitiveStore::open"]

[[protected_file]]
path = "codex-rs/hepta-agentd/src/app_runtime.rs"
required = ["production_writer_host.and_then", "host.production_mutation", "hepta_cognitive_production_mutation"]
forbidden = ["ProductionDurableWriter", "CognitiveStore::open_with_recovery"]

[[protected_file]]
path = "codex-rs/ext/hepta-memory/src/cognitive/tools.rs"
required = ["ProductionCognitiveMutation", "production_mutation", "hepta_cognitive_write_authority_unavailable"]
forbidden = ["ProductionDurableWriter", "CognitiveStore::open_with_recovery"]

[[protected_file]]
path = "codex-rs/hepta-agentd/src/runtime.rs"
required = ["config.take_production_operations()", "operations.attach(Arc::clone(recovered_host)).await?", "attach_production_operations", "run_production_operation_reconciler"]
forbidden = ["ProductionAuthorityToken::from_verified_bytes", "SigningKey::from_bytes"]

[[protected_file]]
path = "codex-rs/hepta-learning-artifacts/src/owner_service.rs"
required = ["LearningArtifactOwnerHost::open", "LearningArtifactOwnerHost::open_with_required_current_head", "recover_current_registry", "current_registry_view", "recovery_required_operations"]
forbidden = ["permit_all", "AllowVerifier", "AuthorityPosture::ALLOW_ALL"]

[[protected_file]]
path = "codex-rs/hepta-learning-artifacts/src/owner_host.rs"
required = ["writer_fence.try_lock", "verify_signature", "verify_current_registry_view", "recover_registry_by_head", "discover_current_head", "current_registry_view", "persist_checkpoint"]
forbidden = ["permit_all", "AllowVerifier"]

[[protected_file]]
path = "codex-rs/hepta-agentd/src/cognitive_ranker.rs"
required = ["VerifiedCurrentRegistryViewV1", "CurrentCognitiveRegistry", ".with_current(current"]
forbidden = ["Result<(File, RegistrySnapshotReceipt)", "candidate.with_current(file, receipt"]

[[protected_file]]
path = "codex-rs/hepta-learning-artifacts/src/selection.rs"
required = ["ArtifactSelectionVerifierV1", "OwnerTrustMismatch", "AuthorityKeyCollision", "AuthorityPosture::DENY_ALL", "load_selected_candidate"]
forbidden = ["AuthorityPosture::ALLOW_ALL", "force_select", "self_select"]

[[protected_file]]
path = "codex-rs/model-provider/src/provider_effect.rs"
required = ["impl ProviderEffectAdapter for HttpProviderEffectAdapter"]
forbidden = ["impl Default for HttpProviderEffectAdapter"]

[[protected_file]]
path = "codex-rs/hepta-contracts/src/final_use.rs"
required = ["pub fn open_state_dir(", "pub fn open_state_dir_with_trust(", "pub fn open_state_dir_with_issuer_keys(", "FinalUseIssuerTrustKey", "pub fn claim_final_use(", "pub fn deliver_final_use", "pub fn deliver_final_use_with_witness", "pub fn dispatch_final_use", "pub fn dispatch_final_use_with_witness", "pub fn claim(", "pub fn with_verified_use", "pub fn with_dispatch_boundary", "FinalUseFrontier", "VerifiedUseTokenWitnessV1", "verify_strict", "validate_live"]
forbidden = ["impl Default for FinalUseAuthority", "pub fn permit_all"]

[[protected_file]]
path = "codex-rs/hepta-contracts/src/final_use_control.rs"
required = ["pub struct FinalUseApprovalVerifier", "pub struct FinalUseRevocationFeedVerifier", "pub struct FinalUseRevocationConvergenceVerifier", "pub struct FinalUseTrustKey", "pub fn new_with_keys(", "RevocationFeedStale", "verify_strict", "update_revocations", "missing_nodes"]
forbidden = ["impl Default for FinalUseApprovalVerifier", "impl Default for FinalUseRevocationFeedVerifier", "permit_all"]

[[protected_file]]
path = "codex-rs/hepta-contracts/src/authority_lease.rs"
required = ["pub struct AuthorityLeaseRegistry", "pub struct AuthorityLeaseVerifier", "pub fn open_state_dir_with_trust(", "pub fn verifier(", "pub fn verify_use(", "pub fn with_dispatch_boundary<T>(", "pub fn deliver_authority_lease_with_witness", "pub fn dispatch_authority_lease_with_witness", "VerifiedUseTokenWitnessV1", "pub fn revoke(", "pub fn prune_expired_leases(", "retired_revisions", "AntiRollbackViolation", "pub fn advance_epoch("]
forbidden = ["impl Default for AuthorityLeaseRegistry", "permit_all"]

[[protected_file]]
path = "codex-rs/hepta-contracts/src/authority_trust.rs"
required = ["pub struct AuthorityDispatchBinding", "pub fn bind_dispatch(", "pub fn dispatch<T>(", "pub trait ProductionAuthorityClock", "pub trait ProductionAuthorityFrontierStore", "pub struct ProductionAuthorityTrustEvidence", "pub fn open_production_state_dir"]
forbidden = ["impl ProductionAuthorityClock for SystemAuthorityClock", "permit_all"]

[[protected_file]]
path = "codex-rs/hepta-fleet/src/authority_port.rs"
required = ["AuthorityDispatchBinding", ".bind_dispatch(", "dispatch_binding", ".dispatch(", "issue_with_witness"]
forbidden = [".verify_use(", "dispatch_authority_lease_with_witness"]

[[protected_file]]
path = "codex-rs/hepta-bao-adapter/src/https_consumer.rs"
required = ["pub async fn consume_kv_v2", "claim_final_use(authority, grant, &binding)", "deliver_final_use(authority, verified, &binding"]
forbidden = ["impl Default for BaoClient", ".claim(grant, &binding)", ".with_verified_use(verified"]

[[protected_file]]
path = "codex-rs/hepta-bao-adapter/src/final_use_host.rs"
required = ["pub struct RegisteredBaoConsumer", "pub struct BaoFinalUseHost", "pub fn apply_revocation_update", "pub async fn consume_kv_v2", "StaleRevocationFeed", "AuthorityClock", "UnregisteredConsumer", "approval_verifier", "revocation_verifier"]
forbidden = ["impl Default for BaoFinalUseHost", "permit_all"]

[authority]
runtime_authority = false
production_caller = false
production_writer = false
external_effect = false
self_issued_acceptance = false
promotion = false
release = false

[[boundary]]
id = "final_use_async_dispatch_fence"
call_pattern = 'authority\s*\.\s*with_verified_use_async(?:_with_witness)?\s*\('
symbol = "FinalUseAuthority::with_verified_use_async"
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub async fn with_verified_use_async", "pub async fn with_verified_use_async_with_witness", "DispatchInProgress", "active_dispatches"]
product_callers = ["codex-rs/hepta-automation/src/authorized_effect.rs"]
caller_markers = ["with_verified_use_async_with_witness", "record_effect_dispatch_authority_witness", "expected_binding"]

[[boundary]]
id = "automation_taskflow_provider_effect_bridge"
symbol = "ProviderEffectTaskFlowDriver::new"
definition_path = "codex-rs/hepta-automation/src/authorized_effect.rs"
definition_markers = ["pub struct ProviderEffectTaskFlowDriver", "pub async fn execute_authorized_taskflow_effect_async", "ProviderEffectKey::for_logical_effect"]
product_callers = []
caller_markers = []

[[boundary]]
id = "final_use_guarded_effect"
symbol = "FinalUseAuthority::with_verified_effect"
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn with_verified_effect", "enter_verified_effect", "active_dispatches"]
product_callers = []
caller_markers = []

[[boundary]]
id = "final_use_async_entry"
symbol = "VerifiedUseToken::enter / FinalUseAuthority::enter_verified_use"
call_pattern = '(?:\bverified_use|\btoken)\s*\.\s*enter\s*\(|\.\s*enter_verified_use\s*\(|VerifiedUseToken\s*::\s*enter\s*\(|FinalUseAuthority\s*::\s*enter_verified_use\s*\('
caller_type_marker = "VerifiedUseToken"
definition_path = "codex-rs/hepta-contracts/src/final_use.rs"
definition_markers = ["pub fn enter(self", "pub fn enter_verified_use(", "StaleRevocationHead"]
product_callers = ["codex-rs/hepta-infer-worker-host/src/native_app_server.rs"]
caller_markers = ["VerifiedUseToken", "verified_use.enter", "EnteredUseToken"]

[[boundary]]
id = "bao_authbus_final_use_consumer"
symbol = "BaoClient::consume_kv_v2_with_authbus"
call_pattern = '\.\s*consume_kv_v2_with_authbus\s*\('
definition_path = "codex-rs/hepta-bao-adapter/src/https_consumer.rs"
definition_markers = ["pub async fn consume_kv_v2_with_authbus", "mark_dispatch_attempted", "consume_kv_v2(authority"]
product_callers = []
caller_markers = []

[[protected_file]]
path = "codex-rs/hepta-agentd/src/production_writer_host.rs"
required = ["impl AgentdProductionOperationRuntimeConfig", "pub(crate) async fn attach(", "recovered_host.writer.verify_current_authority().await?", "recovered_host.production_mutation().is_none()"]
forbidden = []
