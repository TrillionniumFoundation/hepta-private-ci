{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Resume an interrupted turn without submitting new user input.",
  "properties": {
    "threadId": {
      "description": "Thread that owns the interrupted turn.",
      "type": "string"
    },
    "turnId": {
      "description": "Identifier of the interrupted turn to resume.",
      "type": "string"
    }
  },
  "required": [
    "threadId",
    "turnId"
  ],
  "title": "TurnRecoverParams",
  "type": "object"
}