[package]
edition.workspace = true
license.workspace = true
name = "codex-hepta-bellman-operator"
version.workspace = true
publish = false

[lib]
name = "codex_hepta_bellman_operator"
path = "src/lib.rs"
doctest = false

[features]
default = []
# Explicit qualification-only raw input/prediction surface; never enabled by
# a production dependency. Product consumers use owner materialization and pins.
unchecked-qualification-inputs = []

[lints]
workspace = true

[dependencies]
codex-hepta-learning-ledger = { path = "../hepta-learning-ledger" }
codex-hepta-types = { path = "../hepta-types" }

[dev-dependencies]
ed25519-dalek = { workspace = true }
