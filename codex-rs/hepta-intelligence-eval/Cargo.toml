[package]
edition.workspace = true
license.workspace = true
name = "codex-hepta-intelligence-eval"
version.workspace = true
publish = false

[lib]
name = "codex_hepta_intelligence_eval"
path = "src/lib.rs"
doctest = false

[features]
default = []
# Explicit compatibility escape hatch only. Production admission must use the
# signed evaluation entry points documented in PRODUCTION_CONTRACT.md.
legacy-inprocess-eval = []

[lints]
workspace = true

[dependencies]
codex-hepta-learning-ledger = { path = "../hepta-learning-ledger" }
codex-hepta-types = { path = "../hepta-types" }

[dev-dependencies]
ed25519-dalek = { workspace = true }
pretty_assertions = { workspace = true }
