name: Inference worker materializer probe

on:
  push:
    branches:
      - work/product-convergence-20260923
    paths:
      - .github/workflows/hepta-inference-worker-resource-lifecycle.yml

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-24.04
    steps:
      - name: Confirm workflow parsing
        run: echo parsed
