# Lane E final exact-head v3

This human-authored marker establishes the final candidate after residual
remediation. All earlier source-head and synthetic-merge results are stale for
merge purposes.

The exact v3 commit must pass closed-world traceability, locked all-target
compilation, every Lane E owner regression, the cross-crate causal-learning
closure, strict Clippy, rustfmt, clean-tree verification and an ordered-parent
synthetic merge against the current integration base. Ordinary GitHub branch
protection remains authoritative and cannot be bypassed.

External evidence is deliberately not synthesized by this repository. Product
writers, authenticated live outcomes, future-calendar windows, physical erasure
and backup non-resurrection, independent operator acceptance, selection,
promotion, deployment and release remain fail-closed until evidence owners bind
valid receipts to the exact candidate and target environment.
