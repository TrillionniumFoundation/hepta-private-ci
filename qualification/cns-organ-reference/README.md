# CNS organ deterministic reference

Dependency-free reference for organ graph validation, lifecycle, homeostasis, sensor and body generation checks, reflex veto, effect reconciliation, plan selection, topology proposals and revoked-row exclusion. It has no production, network, tool, model or physical-effect authority.

```bash
python3 -m unittest discover -s qualification/cns-organ-reference -p "test_*.py" -v
```
