# learning.eval verification record

This record is an evidence index, not a self-issued deployment authorization.
The normative production contract is
`codex-rs/hepta-intelligence-eval/PRODUCTION_CONTRACT.md`.

## Current source state

- Module: `learning.eval`
- Lane: `LANE-E-LEARNING`
- Source state while this change is under qualification: `source_implemented_ci_pending`
- Promotion / release authority: none
- Production external evaluation ingress: `decide_with_signed_evidence_v2`
- Production longitudinal ingress: `decide_with_signed_longitudinal_evidence_v3`
- Multi-host final-holdout ownership contract: `FencedFinalHoldoutStoreV1`

## Exact-head evidence required for source closure

The Lane E workflow must retain evidence for the exact candidate commit proving:

1. the production boundary verifier passes;
2. `codex-hepta-intelligence-eval` builds and passes all tests, including
   `production_qualification_e2e`;
3. the evaluated-shadow runtime consumer passes its signed-admission and
   no-effect-on-failure tests;
4. strict clippy passes for `codex-hepta-intelligence-eval` and
   `codex-hepta-intelligence`;
5. the maintained Lane E coverage and stress requirements pass where declared;
6. a provenance manifest binds source commit/tree, build identity, source/test
   digests, workflow run/attempt, and generated test artifacts;
7. synthetic-merge qualification succeeds under the Lane E workflow.

The retained provenance manifest is generated by
`scripts/hepta-learning-eval-production.py provenance` and is uploaded under a
commit-addressed artifact name.

## Fail-closed invariants

- The legacy scalar comparator is crate-internal and non-admissible.
- Direct unsigned V1/V2 bundles are trusted-process/test composition only.
- External qualification verifies signed generator/evaluator evidence and role
  separation before producing a decision.
- Eligibility grants no promotion or release authority.
- Final-holdout replay and ownership state cannot be advanced in memory unless the
  configured durable CAS succeeds.
- Multi-host owners are fenced by a monotonically newer token and stale writers are
  rejected.

## External evidence not self-certifiable by this repository

Real future-window efficacy, independent measurements, production trust-root
operation, retention/unlearning evidence, independent selector acceptance, canary
promotion and release remain external claims. Passing source qualification does not
convert those claims into repository facts.
